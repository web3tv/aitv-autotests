import { mergeTests, test } from '@playwright/test';

// src/testWithSynpress.ts
function testWithSynpress(customFixtures) {
  return mergeTests(test, customFixtures);
}

// src/cypress/ensureRdpPort.ts
function ensureRdpPort(args) {
  const existing = args.find((arg) => arg.slice(0, 23) === "--remote-debugging-port");
  if (existing) {
    return Number(existing.split("=")[1]);
  }
  const port = 4e4 + Math.round(Math.random() * 25e3);
  args.push(`--remote-debugging-port=${port}`);
  return port;
}

export { ensureRdpPort, testWithSynpress };
//# sourceMappingURL=out.js.map
//# sourceMappingURL=index.js.map